# 二次开发

二次开发应该让新贡献者容易理解、容易测试、容易回滚。优先使用扩展点，而不是直接改动稳定的默认启动文件。

## 推荐流程

1. 先确认修改属于哪个包。
2. 优先增加参数文件或 launch 参数。
3. 文档中说明 topic、frame 和参数含义。
4. 先在 RViz 中验证，再上车测试。
5. 代码和文档一起提交。

## 扩展示例

- 新增传感器驱动 launch。
- 新增导航参数 profile。
- 新增 RViz 调试视图。
- 新增地图和场景说明。

## 文档规则

所有影响用户使用方式的改动，都应该同步更新这个文档仓库。
